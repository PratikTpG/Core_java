package MethodOverridding;

public class Docs {

    /* ---------------------Method Overriding----------------
        #use to achieve runtime polymorphism
        #There must IS A relationship(inheritance) between classes
        #method name, return type and list of arguments same as parent clas but implementation
           is different in child class is called method overriding.
          * inherit : child class uses same as parent class
          * override : change parent class method in child class

     */
}

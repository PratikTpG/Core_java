package Array;

public class ArrayDocs {
    public static void main(String[] args) {
       /* -----------------Array------------------
       #Collection of Homogeneous data.
       #colleciton/group of multiple values of same data type with fixed size.
       #data stored on the basis of indecis(index start from 0)
       #contains duplicate data.
       #random access
       #contiguos memory allocation.
       #array is an object in java.

            #Syantax
                 data_type array_name[] = new datatype[size];

              e.g.
                 int age[] = new int[500];

                 char aplhs[] = new char[26];

                 string cntrs[] = new string [20]
        */
    }
}
